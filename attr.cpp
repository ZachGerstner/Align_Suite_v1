#include"nw.h"
void sequence::get_attribute_seq()
{
	fprintf(stderr, "WORKED\n");
	/*int dec;
	fprintf(stdout, "How would you like to enter the sequences of interest?\n1. Manual Entry\n2. Upload file\n");
	cin>>dec;
	assert(dec != 0);
	if(dec == 1)
	{
		fprintf(stdout, "Please enter sequence 1:\n");
		cin>>seq.x;
		assert(seq.x != NULL);
	*/
}

void compute::get_attribute_math()
{
	fprintf(stderr, "WORKED!\n");
}
